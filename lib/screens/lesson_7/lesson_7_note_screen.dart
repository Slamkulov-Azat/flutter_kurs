import 'package:flutter/material.dart';

class BMICalculatorNotes extends StatelessWidget {
  const BMICalculatorNotes({Key key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}