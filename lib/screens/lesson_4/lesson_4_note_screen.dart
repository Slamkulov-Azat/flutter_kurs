import 'package:flutter/material.dart';

class DiceNotes extends StatelessWidget {
  const DiceNotes({Key key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}