import 'package:flutter/material.dart';

class XylophoneNotes extends StatelessWidget {
  const XylophoneNotes({Key key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}